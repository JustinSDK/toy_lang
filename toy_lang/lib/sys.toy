exports = ['currentTimeMillis', 'loadedModules', 'unhandledExceptionHandler']

__currentTimeMillis = nativeFunction('currentTimeMillis', this)
def currentTimeMillis() {
    return __currentTimeMillis()
}

__loadedModules = nativeFunction('loadedModules', this)
def loadedModules() {
    return __loadedModules()
}

__unhandledExceptionHandler = nativeFunction('unhandledExceptionHandler', this)
def unhandledExceptionHandler(handler) {
    return __unhandledExceptionHandler(handler)
}

