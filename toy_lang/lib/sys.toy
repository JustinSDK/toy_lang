exports = ['currentTimeMillis', 'loadedModules']

__currentTimeMillis = nativeFunction('currentTimeMillis', sys)
def currentTimeMillis() {
    return __currentTimeMillis()
}

__loadedModules = nativeFunction('loadedModules', sys)
def loadedModules() {
    return __loadedModules()
}

