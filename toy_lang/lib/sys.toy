exports = ['currentTimeMillis', 'loadedModules']

__currentTimeMillis = nativeFunction('currentTimeMillis', this)
def currentTimeMillis() {
    return __currentTimeMillis()
}

__loadedModules = nativeFunction('loadedModules', this)
def loadedModules() {
    return __loadedModules()
}

